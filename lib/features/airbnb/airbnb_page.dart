import 'package:flutter/material.dart';

class AirBnbPage extends StatelessWidget {
  const AirBnbPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.blue[300],
      child: Center(child: Text('Airbnb')),
    );
  }
}
